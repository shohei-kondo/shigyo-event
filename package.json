{
  "name": "shigyo-event",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "cross-env DEPLOY_TARGET=production astro build",
    "build:pages": "cross-env DEPLOY_TARGET=pages astro build",
    "build:prod": "npm run build",
    "preview": "astro preview",
    "preview:prod": "cross-env DEPLOY_TARGET=production astro preview --host 127.0.0.1 --port 4321",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test --config=playwright.config.ts",
    "test:all": "vitest run && playwright test --config=playwright.config.ts",
    "baseline:capture": "playwright test --config=playwright.capture.config.ts"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "cross-env": "^7.0.3",
    "vitest": "^3.0.9"
  }
}
